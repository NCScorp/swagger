# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
        - Filesystem
        - Symfony:
          app_path: 'app'
          var_path: 'var'
          environment: 'test'